# Kotlin serialization supplies its own consumer rules. Keep model names only
# for readable local crash reports; API JSON names are fixed with @SerialName.
-keepattributes SourceFile,LineNumberTable
